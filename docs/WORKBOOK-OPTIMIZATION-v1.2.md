# v1.2：51质量复盘工作簿针对性优化

本轮以用户提供的《高2024级51质量复盘（新版）赋分3(1).xlsx》为本地验收依据。保留质量慧析 new 原有界面，发布到独立的 `/accuracy-v1.2/`。原表和学生信息不提交到仓库。

## 已完成

- 区分有效0分、空白、缺考、缓考、不适用、公式错误、异常分值；有效源总分保留，单科缺失不按0分计算。
- 禁止使用不完整学科小计重建总分；异常身份字段、负分及不合理上限不进入正式统计。入口考试不套用750分总分和150/100分单科上限。
- 保存异常单元格的工作表、源行、源列和原值；规则与班型页面可按当前考试、类别、班级查看，Excel分析包完整导出。
- 同考试、同班、同名的重复记录保留候选来源；成绩相同合并并保留最后排名；成绩冲突整组暂不统计，等待核对原表后重导入。
- 上线指标显式记录可用/部分可用/缺失/空范围、可算人数、排除人数；缺失值用null，显示“—”，不参与强弱排序。
- Excel“学生明细”导出全部筛选学生，另设完整临界生表；Word与PDF取消100人/20人截断，Word知识点取消60项截断。
- PDF按表格行分页并重复表头，每页独立生成图片，避免整个长报告一次生成超大画布。
- v1.2使用独立浏览器数据和考试选择存储，保留此前版本的数据。

## 原表验收基准

| 范围 | 人数 | 总分均值 | 一本/特控 | 本科 |
|---|---:|---:|---:|---:|
| 51物理类 | 433 | 403.5713922320411 | 67 | 237 |
| 51历史类 | 289 | 419.14728192175136 | 34 | 175 |
| 51合计 | 722 | — | 101 | 412 |

- 17班32人，18班19人；51共有34个应考单科空白（物理类18、历史类16），总分全部有效，保留722人。
- 51没有小题答题数据；4册9班地理小题44条；英语小题不复制成日语。
- 归一化考试名称后，历史记录24组重复：23组成绩或原始考试类别冲突，1组成绩相同。比仅按原始考试名检查多1组，因为该组分别写作“41物”和“41历”。这些历史冲突暂不统计，不影响51。
- 合成用例沿用原表33行表头、原分/赋分布局，覆盖0分、空白、缺考、缓考、Excel错误单元格、负数、超上限、非整数班号、缺失总分、重复冲突，以及163名临界学生的完整Excel/Word导出。

## 复现

```sh
npm ci
npx tsc --noEmit
npm run lint
npm run test:accuracy
npm run test:workbook
npm test
# 原表仅在本地传入，不纳入Git：
node --import tsx scripts/verify-accuracy.ts /path/to/workbook.xlsx
node --import tsx scripts/verify-workbook.ts /path/to/workbook.xlsx
```

## 后续操作清单（本轮未完成）

1. **历史身份核对**：根据异常源行检查23组冲突，尤其原始物理/历史类别不一致者；确认学籍号后建立跨考试稳定身份。验收：每组冲突有依据且重导入后消失，不以姓名自动猜测。
2. **班型核对**：确认17、18班班型及历史转班规则，补充考试生效范围。验收：班型对标只匹配当次真实同类班。
3. **学科状态补录**：核对51的34个空白，确定缺考、缓考或漏录。验收：源表状态明确，单科有效人数有依据。
4. **试卷元数据**：逐考试补充满分、赋分规则；逐题补充满分与知识点。验收：51没有题目时不生成小题诊断，录入后可按证据计算。
5. **报告能力**：继续统一Word/PDF的知识点与行动章节，增加摘要/完整选项、生成进度与取消操作；本轮只保证已有名单不截断。
6. **数据管理**：提供导入任务取消、数据清除、版本迁移和匿名导出；当前版本需要单独导入工作簿。
7. **指标补全**：向所有描述统计推广显式缺失状态，并完善每个图表的分母说明；本轮主要处理上线指标。

此前25项路线图仍是长期清单，本轮仅完成与此原表直接相关的一批；未将长期任务整体标记完成。
